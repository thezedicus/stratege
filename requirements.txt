# BiziApp v4.2 — Dependances Streamlit Cloud
# Python 3.11

# Core
streamlit>=1.35.0,<2.0.0

# HTTP & parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Crypto (auth system — hashlib est built-in Python, pas besoin)
# Tous les autres modules (auth_system, api_layer, pricing_plans) sont locaux
